import styled from "styled-components";

const PostBody = styled.article``;

const ShortDescription = styled.p`
  margin-bottom: 3rem;
  text-align: center;
  opacity: 0.5;
`;

export { PostBody, ShortDescription };
